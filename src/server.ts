/**
 * Placeholder MCP server entry. Wired up in T-002.
 * Kept minimal so T-001 ships a working toolchain without prejudging T-002's shape.
 */
export async function main(): Promise<void> {
  // No-op. T-002 replaces this with MCP server bootstrap.
}
